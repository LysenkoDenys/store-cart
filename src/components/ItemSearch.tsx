import TextField from '@mui/material/TextField';

const ItemSearch = () => {
  return (
    <TextField
      label="Search"
      fullWidth
      margin="dense"
      onChange={() => {}}
    ></TextField>
  );
};

export default ItemSearch;
